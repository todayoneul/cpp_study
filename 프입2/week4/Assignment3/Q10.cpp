#include <iostream>
#include <vector>
using namespace std;

int main(){
    int* ptr;
    vector<int> vec;
    int ival;

    // ptr != 0 && *ptr++
    // ival ++ && ival
    // vec[ival++] <= vec[ival]
}